version: "2"

sql:
- schema: "postgres/schema.sql"
  queries: "postgres/query.sql"
  engine: "postgresql"
  gen:
    go:
      package: "pgdbgenerated"
      out: "postgres/generated"
      sql_package: "pgx/v5"
